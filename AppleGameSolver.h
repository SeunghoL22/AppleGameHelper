#ifndef APPLE_GAME_SOLVER_H
#define APPLE_GAME_SOLVER_H

#include <vector>
#include <utility>

// 사과 묶음을 찾는 함수 선언
std::vector<std::pair<int, int>> findAppleBundle(std::vector<std::vector<int>>& grid);

#endif // APPLE_GAME_SOLVER_H